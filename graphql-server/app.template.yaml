runtime: nodejs10

# service: graphql-server or graphql-server-staging
service: ${env:APP_ENGINE_GRAPHQL_SERVER_SERVICE}

env_variables:
  PRISMA_ENDPOINT: ${env:PRISMA_ENDPOINT}
  PRISMA_SERVICE_SECRET: ${env:PRISMA_SERVICE_SECRET}
