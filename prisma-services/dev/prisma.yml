# endpoint: https://eu1.prisma.sh/ci-test/my-app/dev

# hard-coded here so that it's easier to use during dev,
# as clients don't need to mess around with env vars
endpoint: http://prisma-server.dferber.de:4466/my-app/dev
datamodel: ../datamodel.prisma

generate:
  - generator: javascript-client
    output: ../../generated/prisma-client/
